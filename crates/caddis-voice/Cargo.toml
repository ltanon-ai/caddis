[package]
name = "caddis-voice"
version = "0.1.0"
edition = "2021"
description = "VOICE ORGAN P1+P2 (T-35 verdict): detection ladder, voice sets, registry, port hard mutex, /health VRAM, Job Objects, warden-gated config IO; the LISTENING HORN (adopt-don't-duplicate whisper supervision, guarded /transcribe); the ADAPTER half — GA1 dial-time endpoint authorization, GA2 audio validation, GA3 circuit breaker, offline piper adapter under kill-on-close jobs, direct edge-tts protocol + LIVE-PROVEN schannel WSS transport (renegotiation + ALPN + muid/143-era DRM constants; 21888-byte MP3 rendered over :443), earcon set with the R-B substituted/degrade motifs. Zero runtime deps, sync, std only."

# Origin: OMP sergeant BUILD-QUEUE caddis-voice-organ, T-35 amendment ladder
# (state/briefs/t35-voice-amendment-quorum/VERDICT.md + the operator-approved
# plan record: QQ1 auto-kill, QQ2 VRAM-before-spawn, QQ3 Job Objects).

[dependencies]

[dev-dependencies]
