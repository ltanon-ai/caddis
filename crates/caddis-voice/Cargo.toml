[package]
name = "caddis-voice"
version = "0.1.0"
edition = "2021"
description = "VOICE ORGAN P1 (core+OS) + P2 HORN: language detection ladder, voice sets, registry, port hard mutex, /health VRAM, Job Objects, warden-gated config IO — and the LISTENING HORN: adopt-don't-duplicate supervision of whisper-server.exe (port-bound liveness, pid+image identity, strangers refused, adopted engines never killed), token+Host+size guards, multipart dialect, GA2-validated engine client, daemon-contract /transcribe (single-flight 429, WAV sanity, allowlisted path source), capped thread-per-connection httpd. Zero runtime deps, sync, std only."

# Origin: OMP sergeant BUILD-QUEUE caddis-voice-organ, T-35 amendment ladder
# (state/briefs/t35-voice-amendment-quorum/VERDICT.md + the operator-approved
# plan record: QQ1 auto-kill, QQ2 VRAM-before-spawn, QQ3 Job Objects).

[dependencies]

[dev-dependencies]
