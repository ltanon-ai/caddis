[package]
name = "caddis-deliberate"
version = "0.1.0"
edition = "2021"
description = "DELIBERATION ORGAN P0 substrate (BUILD-QUEUE r2-organs-rewrite; plan state/briefs/caddis-deliberate-organ-plan-2026-08-28.md): pure Seat/Panel types + panel construction with floors as DATA (>=2 families, >=1 non-Chinese), free-first ordering, fail-closed refusals. Zero I/O, zero dispatch, zero daemon knowledge; serde is dev-only for the P0 round-trip proof."
# Origin: OMP sergeant BUILD-QUEUE r2-organs-rewrite, rung 6 P-phase plan.
#   Quorum verdict: quorum-r2-organs-rewrite/VERDICT.md (2026-08-26,
#   SHIP-WITH-CHANGES). P0 slice 1 = substrate + panel construction; the
#   seat registry (P1), protocols (P2), dispatch (P3), world (P4), parallel
#   run (P5) and cutover (P6) follow as their own tick-sized slices.
#   Organ law: zero runtime deps, sync, std only (caddis-voice/organs
#   precedent); serde/serde_json are DEV-only until the wire format freezes.

[dependencies]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
